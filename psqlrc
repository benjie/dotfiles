-- From http://www.craigkerstiens.com/2015/12/29/my-postgres-top-10-for-2016/

\set QUIET 1
\pset null '¤'

-- Customize prompts
\set PROMPT1 '%[%033[1m%][%/] # '
\set PROMPT2 '... # '

-- Show how long each query takes to execute
\timing

-- Use best available output format
\x auto
\set VERBOSITY verbose
\set HISTFILE ~/.psql_history- :DBNAME
\set HISTCONTROL ignoredups
\set COMP_KEYWORD_CASE upper
\unset QUIET

--set search_path to hookhaven_data, hookhaven_utils, public;
set search_path to public, mindtree_public, mindtree_hidden, mindtree_utils, mindtree_jobs, jemsgraphs_utils, jemsgraphs_public, app_public, app_private, app_hidden, app_jobs, app_utils, app_jobs, a, b, c, d, forum_example, lookups;
